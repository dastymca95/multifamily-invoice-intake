import { PreferencesPanel } from "@/features/profile-settings/components/PreferencesPanel";

/**
 * Preferences sub-route — app-wide defaults (date format, number
 * format, sign-in landing page, dense tables, etc).
 */
export default function PreferencesSettingsPage() {
  return <PreferencesPanel />;
}
